apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-test-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: vsphere-csi-default
  resources:
    requests:
      storage: 1Gi
